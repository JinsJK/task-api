app = "your-frontend-app-name"  # e.g., task-frontend-abc123
primary_region = "fra"

[build]
  builder = "heroku/buildpacks:20"
  buildpacks = ["https://github.com/heroku/buildpack-static"]

[[statics]]
  guest_path = "/app/dist"
  url_prefix = "/"
